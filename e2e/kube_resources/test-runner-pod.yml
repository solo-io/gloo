apiVersion: v1
kind: Pod
metadata:
  labels:
    app: testrunner
  name: testrunner
  namespace: gloo-system
spec:
  containers:
  - image: soloio/testrunner:v0.1
    command:
      - sleep
      - "36000"
    name: testrunner
  restartPolicy: Always