name: EKS Fed Scale Tests
run-name: Fed Scale Tests - ${{ inputs.edge_enterprise_version }}, k8s ${{ inputs.kubernetes-version }}, cl=20, mult=${{ inputs.scale-multiplier }}, validation ${{ inputs.webhook_validation_enabled == true && 'on' || 'off' }}
on:
  workflow_dispatch:
    inputs:
      cluster_base_name:
        description: The base name of the clusters that will be created cicd-<base name>-<cluster num>
        required: true
        default: edgeFedScaleTests
      edge_enterprise_version:
        description: Edge Enterprise version
        required: false
        default: 1.15.0-rc2
      helm-version:
        description: Helm version
        required: true
        default: v3.12.2
      kubernetes-version:
        description: Kubernetes Version
        type: choice
        default: 1.27
        options:
          - 1.23
          - 1.24
          - 1.25 # We usually use helm v3.11.2
          - 1.26
          - 1.27 # We usually use helm v3.12.2
      scale-multiplier:
        description: "The multiplier you want against the resources created - 3x US, 1x VS, AuthConfig, RateLimit"
        required: false
        default: 100 # A default multiplier of 100 creates 300 US, 100 VS, 100 AC, 100 RL
      webhook_validation_enabled:
        description: Webhook validation enabled
        type: boolean
        default: false
      destroy_infra:
        description: Destroy cloud infrastructure on completion
        type: boolean
        default: true

jobs:
  setup-environment:
    uses: solo-io/gloo-eng-test-beds/.github/workflows/build-eng-cicd-env.yaml@main
    with:
      environment: eks-developers-acct
      num-remote-clusters: 20
      job-reference: ${{ github.event.inputs.cluster_base_name }}
      cluster-env-params: "EKS_KUBERNETES_VERSION=${{ github.event.inputs.kubernetes-version }} EKS_MGMT_INSTANCE_TYPE=c6a.4xlarge EKS_WORKER_INSTANCE_TYPE=c6a.2xlarge"
      component: gloo-edge
    secrets:
      GIT_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

  scale-tests:
    runs-on: ubuntu-latest
    needs: setup-environment
    environment: eks-developers-acct
    env:
      GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      KUBECONFIG_FILE: kubeconfig.cicd-${{ github.event.inputs.cluster_base_name }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      #       Download kubeconfig from the setup-environment job:
      #       https://github.com/solo-io/gloo-eng-test-beds/blob/de4c361ed04bb514b4c5db4533f15685aadb1c6f/.github/workflows/build-eng-cicd-env.yaml#L145-L152
      - name: Download kubeconfig
        uses: actions/download-artifact@v3
        id: kubeconfig
        with:
          name: ${{ env.KUBECONFIG_FILE }}
      - run: |
          KUBECONFIG=${{ steps.kubeconfig.outputs.download-path }}/${{ env.KUBECONFIG_FILE }}
          chmod 600 ${KUBECONFIG}
          echo "KUBECONFIG=${KUBECONFIG}" >> ${GITHUB_ENV}

      - name: Install cli dependencies (helm, kubectl, glooctl)
        env:
          HELM_VERSION: ${{ github.event.inputs.helm-version }}
          GLOO_EE_VERSION: ${{ github.event.inputs.edge_enterprise_version }}
        run: |
          ./ci/eks/install-dependencies.sh

      - name: Install Edge
        env:
          GLOO_EDGE_LICENSE_KEY: ${{ secrets.LICENSE_KEY }}
          GLOO_EE_VERSION: ${{ github.event.inputs.edge_enterprise_version }}
          NUM_REMOTE_CLUSTERS: 20
          WEBHOOK_VALIDATION: ${{ github.event.inputs.webhook_validation_enabled }}
          FED: true
        run: |
          ./ci/eks/install-edge.sh

      - name: Run Scale Tests
        id: scale
        env:
          GLOO_EDGE_LICENSE_KEY: ${{ secrets.LICENSE_KEY }}
          NUM_REMOTE_CLUSTERS: 20
          SCALE_MULTIPLIER: ${{ github.event.inputs.scale-multiplier }}
        run: |
          ./ci/eks/fed-data-creation.sh

      - name: Upload results file
        uses: actions/upload-artifact@v3
        with:
          name: ${{ github.event.inputs.cluster_base_name }}-run-output.csv
          path: |
            ${{ env.POST_PROCESS_LOCATION }}
          retention-days: 7

  cleanup-environment:
    uses: solo-io/gloo-eng-test-beds/.github/workflows/cleanup-eng-env.yaml@main
    needs: scale-tests
    if: ${{ github.event.inputs.destroy_infra == 'true' }}
    with:
      environment: eks-developers-acct
      cluster-group: cicd-${{ github.event.inputs.cluster_base_name }}
    secrets:
      GIT_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}