package helpers

const (
	// the default name of the control plane deployment
	DefaultKgatewayDeploymentName = "kgateway"

	// container within the kgateway pod; its name is currently hardcoded
	KgatewayContainerName = "kgateway"
)
