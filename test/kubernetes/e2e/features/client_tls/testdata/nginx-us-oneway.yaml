apiVersion: gloo.solo.io/v1
kind: Upstream
metadata:
  name: nginx-oneway
  namespace: nginx
spec:
  sslConfig:
    oneWayTls: true
    secretRef:
      name: nginx-tls
      namespace: nginx
  kube:
    serviceName: nginx
    serviceNamespace: nginx
    servicePort: 8443
