apiVersion: v1
kind: Service
metadata:
  labels:
    watchedKey: watchedValue
  name: echo-svc
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 80
  selector:
    app: echo
    version: green
