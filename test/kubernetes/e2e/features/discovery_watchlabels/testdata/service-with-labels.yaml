apiVersion: v1
kind: Service
metadata:
  labels:
    watchedKey: watchedValue
    bonusKey: bonusValue
  name: example-svc
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8080
  selector:
    selectorKey: selectorValue
