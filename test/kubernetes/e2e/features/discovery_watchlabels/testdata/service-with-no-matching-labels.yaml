apiVersion: v1
kind: Service
metadata:
  labels:
    watchedKey: unwatchedValue
  name: example-svc-no-matching-labels
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8080
  selector:
    selectorKey: selectorValue
