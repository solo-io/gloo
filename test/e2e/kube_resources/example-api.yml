apiVersion: config.delta.io/v1
kind: Api
metadata:
  namespace: glue-system
  name: hello-api
spec:
  match:
    source:
      address: # for the purpose of whitelist/blacklist
    request:
      verb: GET
  destination:
    service: helloservice
    namespace: glue-system
