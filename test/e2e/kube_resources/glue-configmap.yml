apiVersion: v1
data:
  glue.yml: |
    - example_rule:
        timeout: 15s
        match:
          prefix: /broken
        upstream:
          name: nonexistent
          address: doesntresolve
          port: 1234
kind: ConfigMap
metadata:
  name: glue-config
  namespace: glue-system