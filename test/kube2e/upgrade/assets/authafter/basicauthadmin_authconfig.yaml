apiVersion: enterprise.gloo.solo.io/v1
kind: AuthConfig
metadata:
  name: basic-auth-admin
  namespace: gloo-system
spec:
  configs:
    - basicAuth:
        apr:
          users:
            admin2:
              # generated with: `htpasswd -nbm admin2 password`
              # remove previous admin and add admin2
              hashedPassword: Slwr6oIxIt7hl0TncRkqq/
              salt: pLTiSIjT
        realm: gloo
