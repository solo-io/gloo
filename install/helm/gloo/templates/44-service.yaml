{{- if .Values.gateway2.controlPlane.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gloo-gateway.controlPlane.fullname" . }}
  labels:
    {{- include "gloo-gateway.gateway.const_labels" . | nindent 4 }}
    {{- include "gloo-gateway.controlPlane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.gateway2.controlPlane.service.type }}
  ports:
    - port: {{ .Values.gateway2.controlPlane.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gloo-gateway.controlPlane.selectorLabels" . | nindent 4 }}
{{- end }}