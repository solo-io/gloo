import * as React from 'react';
/** @jsx jsx */
import { jsx } from '@emotion/core';

import styled from '@emotion/styled/macro';
import { colors } from 'Styles';

interface Props {}

export const WatchedNamespacesPage = (props: Props) => {
  return <div>Namesapce</div>;
};
