kind: Gateway
apiVersion: gateway.networking.k8s.io/v1beta1
metadata:
  name: gw
spec:
  gatewayClassName: solo-gateway
  listeners:
  - protocol: HTTP
    port: 80
    name: http
    allowedRoutes:
      namespaces:
        from: Same