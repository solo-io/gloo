# Code generated by skv2. DO NOT EDIT.


# Rbac manifests for multicluster-admission-webhook-test

{{- $multiclusterAdmissionWebhookTest := $.Values.multiclusterAdmissionWebhookTest}}
{{- if $multiclusterAdmissionWebhookTest.enabled }}
---

kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: multicluster-admission-webhook-test
  labels:
    app: multicluster-admission-webhook-test
rules:
- apiGroups:
  - multicluster.solo.io
  resources:
  - multiclusterroles
  - multiclusterrolebindings
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - '*'
- apiGroups:
  - admissionregistration.k8s.io
  resources:
  - validatingwebhookconfigurations
  verbs:
  - get
  - list
  - watch
  - update

---

kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: multicluster-admission-webhook-test
  labels:
    app: multicluster-admission-webhook-test
subjects:
- kind: ServiceAccount
  name: multicluster-admission-webhook-test
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: multicluster-admission-webhook-test
  apiGroup: rbac.authorization.k8s.io

{{- end }}