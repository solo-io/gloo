# Code generated by skv2. DO NOT EDIT.

---

# Deployment manifest for multicluster-admission-webhook-test

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: multicluster-admission-webhook-test
  name: multicluster-admission-webhook-test
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      app: multicluster-admission-webhook-test
  template:
    metadata:
      labels:
        app: multicluster-admission-webhook-test
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "9091"
        prometheus.io/scrape: "true"
    spec:
      serviceAccountName: multicluster-admission-webhook-test
      volumes:
      - name: admission-certs
        secret:
          optional: true
          secretName: multicluster-admission-webhook-test
      containers:
{{- $multiclusterAdmissionWebhookTest := $.Values.multiclusterAdmissionWebhookTest }}
{{- $multiclusterAdmissionWebhookTestImage := $multiclusterAdmissionWebhookTest.Image }}
      - name: multicluster-admission-webhook-test
        image: {{ $multiclusterAdmissionWebhookTestImage.registry }}/{{ $multiclusterAdmissionWebhookTestImage.repository }}:{{ $multiclusterAdmissionWebhookTestImage.tag }}
        imagePullPolicy: {{ $multiclusterAdmissionWebhookTestImage.pullPolicy }}
        env:
          - name: POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          - name: SERVICE_NAME
            value: multicluster-admission-webhook-test
          - name: SECRET_NAME
            value: multicluster-admission-webhook-test
          - name: VALIDATING_WEBHOOK_CONFIGURATION_NAME
            value: multicluster-admission-webhook-test
          - name: CERT_DIR
            value: /etc/certs/admission
          - name: WEBHOOK_PATH
            value: /admission
        volumeMounts:
        - mountPath: /etc/certs/admission
          name: admission-certs
          readOnly: true
{{- if $multiclusterAdmissionWebhookTest.Resources }}
        resources:
{{ toYaml $multiclusterAdmissionWebhookTest.Resources | indent 10}}
{{- else}}
        resources:
          requests:
            cpu: 500m
            memory: 256Mi
{{- end}}
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        {{- if $multiclusterAdmissionWebhookTestImage.pullSecret }}
        imagePullSecrets:
        - name: {{ $multiclusterAdmissionWebhookTestImage.pullSecret }}
        {{- end}}

---

# Service account for multicluster-admission-webhook-test

apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: multicluster-admission-webhook-test
  name: multicluster-admission-webhook-test
  namespace: {{ $.Release.Namespace }}



---
# Service for multicluster-admission-webhook-test

apiVersion: v1
kind: Service
metadata:
  labels:
    app: multicluster-admission-webhook-test
  name: multicluster-admission-webhook-test
  namespace: {{ $.Release.Namespace }}
spec:
  selector:
    app: multicluster-admission-webhook-test
  type: ClusterIP
  ports:
  - name: webhook
    port: 8443



