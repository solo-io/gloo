type: static
name: nats-streaming
metadata:
  annotations:
    gloo.solo.io/service-type: nats-streaming
spec:
  hosts:
  - addr: nats-streaming
    port: 4222
  service_info:
    type: nats-streaming