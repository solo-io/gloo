changelog:
  - type: NON_USER_FACING
    issueLink: https://github.com/solo-io/solo-projects/issues/5037
    resolvesIssue: false
    description: |
      Don't attempt to publish files (gloo-gateway.yaml, gloo-ingress.yaml, gloo-knative.yaml) that 
      are not generated by our pipeline. These caused releases to fail, because we would attempt to
      publish a file that was not found locally.