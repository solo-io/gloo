changelog:
  - type: NEW_FEATURE
    description: Allow providing a `--kubeyaml` flag to glooctl commands to dump the associated kubernetes yaml objects
    issueLink: https://github.com/solo-io/gloo/issues/616
