changelog:
  - type: NEW_FEATURE
    issueLink: https://github.com/solo-io/gloo/issues/7377
    description: Ports in helm charts can now be disabled via setting the port to 0 (or via toggle if port hardcoded).
