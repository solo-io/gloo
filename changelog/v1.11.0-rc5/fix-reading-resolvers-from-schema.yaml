changelog:
  - type: FIX
    description: >
      The resolver wizard now reads the initial state of resolvers from the schema correctly.
    issueLink: https://github.com/solo-io/solo-projects/issues/3446
