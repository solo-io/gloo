changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/gloo/issues/6524
    resolvesIssue: false
    description: |
      Fix helm upgrade failure that occurs when a Proxy is referencing extauth/ratelimit Upstreams provided by the helm chart.
  - type: FIX
    issueLink: https://github.com/solo-io/gloo/issues/6555
    resolvesIssue: false
    description: |
      Fix helm upgrade failure that occurs when trying to use new Gateway fields.
