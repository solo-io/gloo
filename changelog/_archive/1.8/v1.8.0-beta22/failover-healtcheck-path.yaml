changelog:
  - type: NEW_FEATURE
    issueLink: https://github.com/solo-io/gloo/issues/4642
    resolvesIssue: false
    description: (Enterprise only) Adds `path` and `method` options to failover endpoint-specific health check config.