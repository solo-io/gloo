changelog:
- type: FIX
  issueLink: https://github.com/solo-io/gloo/issues/7165
  description: Fixes issue where gRPC plugin is not reset correctly on every run of the translation loop, causing a slow down in gloo performance and envoy memory spikes.