changelog:
  - type: NON_USER_FACING
    issueLink: https://github.com/solo-io/gloo/issues/5085
    description: >
      Add documentation around the breaking change introduced by removing
      `validationServerGrpcMaxSize` from the settings CRD, and replacing it
      with `validationServerGrpcMaxSizeBytes`.