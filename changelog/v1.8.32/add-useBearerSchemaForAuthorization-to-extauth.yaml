changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/gloo/issues/6501
    resolvesIssue: false
    description: | 
      Add useBearerSchemaForAuthorization to toggle if Bearer prefix should be added to token Authorization header.
