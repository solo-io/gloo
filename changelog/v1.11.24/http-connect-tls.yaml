changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/gloo/issues/6743
    description: >
      Add `httpConnectSslConfig` field to `Upstream` to allow users to configure TLS origination
      to the HTTP Connect proxy.
    resolvesIssue: false