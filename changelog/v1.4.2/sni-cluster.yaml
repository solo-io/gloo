changelog:
- type: FIX
  issueLink: https://github.com/solo-io/gloo/issues/3223
  description: >
    Fix TCP multi-cluster routing by enabling routing via SNI name. Gloo adds new APIs to expose the envoy tcp SNI filter.
