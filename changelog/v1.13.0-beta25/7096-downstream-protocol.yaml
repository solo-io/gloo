changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/gloo/issues/7096
    resolvesIssue: false
    description: >
      Differentiate between `use_downstream_protocol_config` and `explicit_http_config` when `useHttp2` is set on an Upstream.
