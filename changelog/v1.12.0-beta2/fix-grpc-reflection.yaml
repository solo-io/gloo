changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/solo-projects/issues/3509
    description: fixes error in gRPC reflection -> GraphQLApis where gRPC reflection client doesn't close gracefully.