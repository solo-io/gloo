changelog:
  - type: NON_USER_FACING
    description: >-
      check.CheckResources() now doesnt accumulate goroutines in the opts.Top.ctx context if called repeatedly such as 
      in an Eventually function.