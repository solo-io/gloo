changelog:
  - type: FIX
    description: >
      Updated envoy-gloo based, which includes fixes to support auto host rewrite on EDS clusters.
      (i.e., fixing auto host rewrite for gloo with consul)
    issueLink: https://github.com/solo-io/gloo/issues/2552