changelog:
  - type: NON_USER_FACING
    description: Change JWT-checking githook to decode potential jwts and check for common json value names before flagging a commit. Also fix a minor unrelated test bug.