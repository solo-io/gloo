changelog:
  - type: NON_USER_FACING
    issueLink: https://github.com/solo-io/solo-projects/issues/6341
    resolvesIssue: false
    description: >-
        Move internal `ClaimToHeader and `Azure` messages up a level of scope for reuse
        Add dynamic_metadata_from_claims to internal AccessTokenValidationConfig interface.
        Add ServerDefaultApiKeyStorage to internal ApiKeyAuthConfig storage_backends.
        Add dynamic_metadata_from_claims, claimsToHeaders, and provider to internal OidcAuthorizationCodeConfig.
        These changes are to support parity with existing GME functionality.
