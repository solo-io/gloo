changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/solo-projects/issues/4760
    resolvesIssue: false
    description: >-
      Fixes an issue when setting proxyprotocol on an upstream that was also static.
  - type: FIX
    issueLink: https://github.com/solo-io/solo-projects/issues/4982
    resolvesIssue: false
    description: >-
      Fixes overlapping sni when using the new matcher framework for filters
  - type: FIX
    issueLink: https://github.com/solo-io/solo-projects/issues/4982
    resolvesIssue: false
    description: >-
      Update to support tcp when isolating on filterchains on virtual services.
      Previously when not isolating filter chains tcp and http could coexist by matcher.
      Now when isolated tcp is not ignored (previously ignored on delegated and warned on inline).
  - type: FIX
    issueLink: https://github.com/solo-io/solo-projects/issues/5044
    resolvesIssue: false
    description: >-
      Add a field for the enterprise passthrough feature. 
      The new field allows for a subsection of ciphers to be considered for tls termination.